programa
{
	inclua biblioteca Matematica
 --> mat
	
real altura, imc, peso
	
	funcao inicio()
	{
		escreva("Digite seu peso: ")
		leia(peso)
		escreva("Digite sua altura: ")
		leia(altura)

		imc = (peso) / (altura * altura)
		escreva("Seu IMC é: " ,mat.arredondar(imc, 3))
	}
}
